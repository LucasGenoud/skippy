<?xml version="1.0" encoding="UTF-8"?>
<!--
  One tickable checklist row. The row itself deliberately has no click action:
  only the checkbox and its label can toggle an item, so touching the empty
  space beside a label never changes the checklist.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:minHeight="48dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- A home screen is read at arm's length and ticked with a thumb, so the
         box and its label are a size up from a compact in-app list. -->
    <ImageView
        android:id="@+id/row_check"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginEnd="10dp"
        android:contentDescription="@string/widget_toggle_item"
        android:src="@drawable/ic_widget_unchecked" />

    <TextView
        android:id="@+id/row_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="18sp"
        android:textColor="@color/widget_text" />

</LinearLayout>
